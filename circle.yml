machine:
  node:
    version: 8.9.3
  
dependencies:
  override:
    - npm install -g eslint

deployment:
  qa:
    branch: ejercicios
    commands:
      - eslint ./
  prod:
    branch: master
    commands:
      - eslint ./