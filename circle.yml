machine:
  node:
    version: 8.9.3
  
dependencies:
  override:
    - npm install -g eslint

test:
  override:
    - cd platzom/
    - npm test

deployment:
  qa:
    branch: ejercicios
    commands:
      - 
  prod:
    branch: master
    commands:
      - eslint ./